using System;

namespace SmartBudgetCommon
{
	public enum DBReccurence
	{
		NEVER,
		EVERY_DAY,
		EVERY_WEEK,
		EVERY_2WEEKS,
		EVERY_MONTH,
		EVERY_2MONTHS,
		EVERY_QUARTER,
		TWICE_A_YEAR,
		EVERY_YEAR,
		RECURRENCE_COUNT
	}
}

